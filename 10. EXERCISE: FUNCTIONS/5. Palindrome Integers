def palindromes(palindrome):
    if palindrome==palindrome[::-1]:
         return True
    else:
         return False


numbers=input().split(", ")
for palindrome in numbers:
    print(palindromes(palindrome))
