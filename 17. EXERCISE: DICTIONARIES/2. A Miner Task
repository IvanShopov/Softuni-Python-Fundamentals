resources = {}
command = input()
while command != "stop":
    quantity = int(input())
    if command not in resources.keys():
        resources[command] = quantity
    else:
        resources[command] += quantity
    command = input()
for key, value in resources.items():
    print(f"{key} -> {value}")
